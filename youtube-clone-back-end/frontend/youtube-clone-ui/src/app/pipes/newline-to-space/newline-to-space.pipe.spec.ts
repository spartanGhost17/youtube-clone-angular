import { NewlineToSpacePipe } from './newline-to-space.pipe';

describe('NewlineToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new NewlineToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
