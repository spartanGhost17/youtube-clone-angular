import { ContainerBgGradiantDirective } from './container-bg-gradiant.directive';

describe('ContainerBgGradiantDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerBgGradiantDirective(null as any);
    expect(directive).toBeTruthy();
  });
});
